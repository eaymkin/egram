# 🚀 Деплой YouTube Clone на Render

Подробная инструкция по развертыванию приложения на Render.

## Предварительные требования

1. Аккаунт на [Render](https://render.com) (бесплатная регистрация)
2. Аккаунт на [GitHub](https://github.com) (для хранения кода)
3. Git установлен локально

## Шаг 1: Подготовка кода

### 1.1 Инициализация Git (если еще не сделано)

```bash
cd "c:\Users\eaymk_mvlkdvh\Новая папка (2)"
git init
git add .
git commit -m "Initial commit - YouTube Clone"
```

### 1.2 Создание репозитория на GitHub

1. Зайдите на https://github.com
2. Нажмите "New repository"
3. Назовите репозиторий (например, `youtube-clone`)
4. Оставьте его публичным или приватным (как хотите)
5. НЕ добавляйте README, .gitignore или license (у нас уже есть)
6. Нажмите "Create repository"

### 1.3 Загрузка кода на GitHub

```bash
# Замените YOUR_USERNAME и YOUR_REPO на ваши данные
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main
```

## Шаг 2: Настройка Backend на Render

### 2.1 Создание Web Service для Django

1. Зайдите на https://dashboard.render.com
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub репозиторий
4. Настройте следующие параметры:

**Основные настройки:**
- **Name**: `youtube-clone-backend` (или любое имя)
- **Region**: Выберите ближайший к вам регион
- **Branch**: `main`
- **Root Directory**: `backend`
- **Runtime**: `Python 3`
- **Build Command**: `./build.sh`
- **Start Command**: `gunicorn config.wsgi:application`

### 2.2 Настройка переменных окружения (Environment Variables)

Добавьте следующие переменные:

```
SECRET_KEY = [Нажмите "Generate" для создания случайного ключа]
DEBUG = False
ALLOWED_HOSTS = youtube-clone-backend.onrender.com
FRONTEND_URL = https://youtube-clone-frontend.onrender.com
```

**ВАЖНО:** Замените `youtube-clone-backend` и `youtube-clone-frontend` на ваши реальные имена сервисов.

### 2.3 Создание PostgreSQL базы данных

1. На той же странице прокрутите вниз до "Environment"
2. Нажмите "Add Database"
3. Выберите "PostgreSQL"
4. Render автоматически создаст БД и добавит `DATABASE_URL` в переменные окружения

### 2.4 Разрешение на выполнение build.sh

После первого деплоя, если возникнет ошибка с правами доступа:
1. Откройте "Shell" в вашем сервисе
2. Выполните: `chmod +x build.sh`

Или добавьте в репозиторий:
```bash
git update-index --chmod=+x backend/build.sh
git commit -m "Make build.sh executable"
git push
```

### 2.5 Деплой Backend

1. Нажмите "Create Web Service"
2. Render начнет деплой (займет 5-10 минут)
3. После завершения скопируйте URL (например: `https://youtube-clone-backend.onrender.com`)

## Шаг 3: Настройка Frontend на Render

### 3.1 Создание Static Site для React

1. Снова нажмите "New +" → "Static Site"
2. Выберите тот же GitHub репозиторий
3. Настройте параметры:

**Основные настройки:**
- **Name**: `youtube-clone-frontend`
- **Branch**: `main`
- **Root Directory**: `frontend`
- **Build Command**: `npm install && npm run build`
- **Publish Directory**: `build`

### 3.2 Настройка переменных окружения для Frontend

Добавьте:
```
REACT_APP_API_URL = https://youtube-clone-backend.onrender.com/api
```

**ВАЖНО:** Замените URL на реальный URL вашего backend!

### 3.3 Деплой Frontend

1. Нажмите "Create Static Site"
2. Render начнет деплой (3-5 минут)
3. После завершения скопируйте URL (например: `https://youtube-clone-frontend.onrender.com`)

## Шаг 4: Финальная настройка

### 4.1 Обновление CORS в Backend

1. Вернитесь к backend сервису на Render
2. Перейдите в "Environment"
3. Обновите `FRONTEND_URL` на реальный URL frontend
4. Обновите `ALLOWED_HOSTS` добавив реальный домен backend
5. Нажмите "Save Changes"
6. Render автоматически передеплоит

### 4.2 Создание суперпользователя

1. В backend сервисе откройте "Shell"
2. Выполните команды:
```bash
cd backend
python manage.py createsuperuser
```
3. Введите username, email и password

## Шаг 5: Тестирование

### 5.1 Проверка работы

1. Откройте URL вашего frontend
2. Зарегистрируйте нового пользователя
3. Создайте канал
4. Попробуйте загрузить тестовое видео (небольшого размера!)

### 5.2 Доступ к админ-панели

Перейдите на: `https://your-backend-url.onrender.com/admin/`

## Важные примечания

### Ограничения бесплатного tier Render:

✅ **Включено:**
- 750 часов работы в месяц
- PostgreSQL база данных (90 дней, потом нужно обновить)
- SSL сертификаты (HTTPS)
- Автоматические деплои при push в Git

❌ **Ограничения:**
- Сервисы "засыпают" после 15 минут неактивности
- Первый запрос после пробуждения займет 30-50 секунд
- Ограничение на размер хранилища для медиа-файлов
- Нет CDN для видео

### Оптимизация для бесплатного tier:

1. **Сжимайте видео** перед загрузкой
2. **Ограничьте размер** загружаемых файлов в коде
3. **Используйте изображения** небольшого размера для миниатюр

## Обновление приложения

После внесения изменений в код:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Render автоматически обнаружит изменения и передеплоит приложение.

## Мониторинг

В dashboard Render вы можете:
- Смотреть логи в реальном времени
- Проверять статус деплоев
- Мониторить использование ресурсов
- Настраивать алерты

## Решение проблем

### Backend не запускается:
1. Проверьте логи в Render dashboard
2. Убедитесь что все переменные окружения настроены
3. Проверьте что `DATABASE_URL` добавлен автоматически

### Frontend не подключается к Backend:
1. Проверьте что `REACT_APP_API_URL` правильный
2. Убедитесь что CORS настроен в backend
3. Проверьте что `FRONTEND_URL` в backend правильный

### Видео не загружаются:
1. Проверьте размер файла (ограничение хостинга)
2. Убедитесь что `MEDIA_ROOT` директория создана
3. Проверьте логи на ошибки загрузки

## Переход на платный план (опционально)

Если приложение будет популярным, рекомендуется:

1. **Starter Plan** ($7/месяц) - сервисы не засыпают
2. **Professional Plan** ($25/месяц) - больше ресурсов
3. Использовать **AWS S3** или **Cloudinary** для медиа-файлов
4. Настроить **CDN** для быстрой доставки видео
5. Использовать **Redis** для кэширования

## Дополнительные улучшения

После успешного деплоя рассмотрите:

- [ ] Настройка custom домена
- [ ] Настройка email уведомлений
- [ ] Добавление Google Analytics
- [ ] Настройка мониторинга (Sentry)
- [ ] Бэкапы базы данных
- [ ] CDN для статических файлов
- [ ] Сжатие видео на сервере (FFmpeg)

---

**Готово!** Ваш YouTube Clone теперь доступен онлайн! 🎉

**Backend URL**: https://your-backend-url.onrender.com
**Frontend URL**: https://your-frontend-url.onrender.com
**Админ**: https://your-backend-url.onrender.com/admin/
